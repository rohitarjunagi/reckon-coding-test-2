type DivisorType = {
  divisor: number;
  output: string;
};

type RangeInfo = {
  lower: number;
  upper: number;
};

type DivisiorInfo = {
  outputDetails: DivisiorType[];
};

// type RetryReturnType = RangeInfo | DivisiorInfo | {};
